name: Sanctuary Portal (Archived)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Archive Notice
        run: |
          echo "Sanctuary Portal workflow archived."
          echo "Deployment is handled by Vercel; no actions executed."
