name: Sanctuary Portal (Archived)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Archive Notice
        run: |
          echo "Sanctuary Portal Azure deployment is deprecated."
          echo "Deployment has moved to Vercel; this workflow does nothing."
