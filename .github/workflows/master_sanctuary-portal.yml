name: Sanctuary Portal (Archived)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Archive Notice
        run: |
          echo "Sanctuary Portal Azure deployment is deprecated."
          echo "This repository now deploys via Vercel."
          echo "No Azure steps are executed by this workflow."
