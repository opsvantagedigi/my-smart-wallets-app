name: CI placeholder - smoke workflow removed

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op placeholder
        run: echo "CI smoke workflow removed; placeholder to clear editor diagnostics"
